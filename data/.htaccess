# Melindungi folder data: file JSON tidak bisa dibuka langsung dari browser.
# Hanya api.php (di server) yang boleh membaca/menulisnya.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
